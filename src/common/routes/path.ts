function path(root: string, sublink: string) {
  return `${root}${sublink}`;
}

const ROOT_PATH_RENT_SPACE = "/";

export const PATH_AUTH = {
  login: "/login",
  register: "/register",
};

export const PATH_RENT_SPACE = {
  root: ROOT_PATH_RENT_SPACE,
};
